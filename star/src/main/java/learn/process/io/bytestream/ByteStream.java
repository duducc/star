package javaprocess.io.bytestream;

/**
 * 字节流
 *
 * 一切皆为字节
 *
 *    我们电脑里面存储的一切文件数据（文本，图片，视频等）在存储时，都是以二进制数字的形式保存在我们的计算机上，都一个一个的字节
 *    因为我们说过计算机它只能识别二进制 也就是  01 01  那么传输时一样如此。所以，字节流可以传输任意文件数据（如把这个图片读取到内存中去，把这个视频读取到内存中去  那它也是以字节的方式来读取的）
 *    在操作流的时候，我们要时刻明确，无论使用什么样的流对象，底层传输的始终为二进制数据
 *
 */
public class ByteStream {
}
