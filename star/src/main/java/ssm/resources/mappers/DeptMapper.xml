<?xml version="1.0" encoding="UTF-8"?> 
	<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN" 
		"http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">
	<mapper namespace="cn.tedu.ssm.mapper.DeptMapper">
	<insert id="insertDept" parameterType="cn.tedu.ssm.bean.Dept">
	  insert into
	    t_dept
	    (dept_name,dept_address)
	    values
	    (#{deptName},#{deptAddress})
	
	</insert>
	<select id="selectAll" resultType="cn.tedu.ssm.bean.Dept">
	  select 
	     id,dept_name deptName,
	     dept_address deptAddress
	    from 
	    t_dept
	
	</select>
	<delete id="delDept">
	 delete  from t_dept where id=#{id}
	</delete>
		<select id="selectById" resultType="cn.tedu.ssm.bean.Dept">
		  select 
		     id,dept_name deptName,
		     dept_address deptAddress
		     from
		      t_dept
		      where
		      id=#{id}
		</select>
		<update id="updateDept" parameterType="cn.tedu.ssm.bean.Dept">
		 update t_dept
		     set
		     dept_name=#{deptName},
		     dept_address=#{deptAddress}		     
		     where
		     id=#{id}
		    
		</update>
		
	</mapper>
	
	
	
	
	
	
	
	
	